namespace Didionysymus.Lattice.Runtime
{
    /// <summary>
    /// The lifetime of a dependency container.
    /// </summary>
    public enum Lifetime
    {
        Transient,
        Scoped,
        Singleton
    }
}